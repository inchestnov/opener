## Agent skills

### Issue tracker

Local markdown under `.scratch/<feature>/` (no git remote configured yet). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
